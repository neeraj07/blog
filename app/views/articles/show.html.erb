<h3>Timeline Post</h3>
     
        <p> 
			<h3><%= @article.title %></h3>
		</p>
	
		<p>
			<%= @article.body %>
		   

			<%= link_to "Edit", edit_article_path(@article) %>
			<!-- 
              <%= button_to "Edit", edit_article_path(@article) 
              %> -->
			<%= link_to "Delete", article_path(@article), method: :delete, data: {confirm: "Really delete the article?"} %></div>
			 <h5>Comments (<%= @article.comments.size %>)</h5>


		</p>
		<p>
          Tags:
           <% @article.tags.each do |tag| %>
            <%= link_to tag.name, tag_path(tag) %>
           <% end %>
        </p>

	

         <p style="width:100%"><%= image_tag @article.image.url %></p>
         <% if logged_in? %>

<% end %>




	
		<%= render partial: 'articles/comment', collection: @article.comments %>
		<%= render partial: 'comments/form', locals: { love: @article.comments }%>


<%= link_to "<< Back to Articles List", articles_path %>
 
	
